/** props(not used) */
export interface HeaderProps {
}

/** User */
export interface User {
  name: string,
  email: string,
  imageUrl: string,
}

/** Common menu item */
export interface MenuItem {
  name: string,
  href: string,
}